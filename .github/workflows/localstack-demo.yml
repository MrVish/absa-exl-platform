name: localstack-demo

on:
  pull_request:
    paths:
      - "code-intake/**"
      - "pipeline-factory/**"
      - "manifest-signer/**"
      - "registry/api/**"
      - "terraform/modules/pipeline-registry/**"
      - "terraform/modules/signing-foundation/**"
      - "platform-contracts/**"
      - "packages/credit-risk-pd/**"
      - "pipelines/credit-risk-pd/**"
      - "pipeline-factory/configs/credit-risk-pd/**"
      - "scripts/demo/**"
      - "infra/localstack/**"
      - ".github/workflows/localstack-demo.yml"
      - "pyproject.toml"
      - "uv.lock"
  push:
    branches: [main]
    paths:
      - "code-intake/**"
      - "pipeline-factory/**"
      - "manifest-signer/**"
      - "registry/api/**"
      - "terraform/modules/pipeline-registry/**"
      - "terraform/modules/signing-foundation/**"
      - "platform-contracts/**"
      - "packages/credit-risk-pd/**"
      - "pipelines/credit-risk-pd/**"
      - "pipeline-factory/configs/credit-risk-pd/**"
      - "scripts/demo/**"
      - "infra/localstack/**"
      - ".github/workflows/localstack-demo.yml"
      - "pyproject.toml"
      - "uv.lock"

concurrency:
  group: localstack-demo-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  demo:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: uv sync
        run: uv sync --frozen --all-extras

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.5"
          terraform_wrapper: false

      - name: Pull LocalStack image
        run: docker pull localstack/localstack:3.8.1
        timeout-minutes: 2

      - name: Run demo
        id: demo
        env:
          PYTHONPATH: scripts
        run: |
          set +e
          uv run python -m demo run --no-color --no-cleanup --transcript demo-transcript.md
          ec=$?
          echo "exit_code=$ec" >> $GITHUB_OUTPUT
          # ALWAYS exit 0; the "Gate on demo exit code" step below decides
          # job status. GitHub aggregates step statuses into the job, so
          # if this step exited with the demo's exit code we'd never be
          # able to convert exit 2/3 (infra failures) into job success.
          exit 0

      - name: Upload transcript on success
        if: steps.demo.outputs.exit_code == '0'
        uses: actions/upload-artifact@v4
        with:
          name: demo-transcript
          path: demo-transcript.md
          retention-days: 14

      - name: Upload failure bundle
        if: steps.demo.outputs.exit_code != '0'
        uses: actions/upload-artifact@v4
        with:
          name: demo-failure-bundle
          path: |
            demo-transcript.md
            infra/localstack/terraform/terraform.tfstate
            infra/localstack/.uvicorn.log
          retention-days: 30
          if-no-files-found: warn

      - name: Gate on demo exit code
        run: |
          ec="${{ steps.demo.outputs.exit_code }}"
          case "$ec" in
            0) echo "::notice::Demo passed (chain verified end-to-end)." ;;
            1) echo "::error::Chain verification failed (platform regression). See demo-failure-bundle artifact." ; exit 1 ;;
            2) echo "::warning::Demo infrastructure failure (not a platform regression); not blocking merge." ;;
            3) echo "::warning::Demo teardown failed (CI runner discarded); not blocking merge." ;;
            *) echo "::error::Demo failed with unexpected exit code $ec" ; exit 1 ;;
          esac

      - name: Always tear down
        if: always()
        run: |
          docker compose -f infra/localstack/docker-compose.yml down -v || true
          rm -f infra/localstack/.uvicorn.pid infra/localstack/.uvicorn.log
